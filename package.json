{
  "name": "make-ready",
  "version": "0.1.0",
  "description": "Musket-line action game with an adaptive Strudel soundtrack",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "build:itch": "ITCH=1 vite build --outDir dist-itch",
    "zip:itch": "npm run build:itch && cd dist-itch && zip -rq ../make-ready-itch.zip . && cd .. && echo \"-> make-ready-itch.zip ready to upload\""
  },
  "dependencies": {
    "@strudel/web": "^1.0.3",
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^6.0.0"
  }
}
